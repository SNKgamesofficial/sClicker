name: Build macOS App

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build app
        run: |
          pyinstaller --windowed --name sClickerV5 sClicker.py

      - name: Upload app
        uses: actions/upload-artifact@v4
        with:
          name: sClickerV5-macOS
          path: dist/sClickerV5.app
